#include "monty.h"

/**
 * m_nop - no operations performed
 * @stack: pointer to head of the stack
 * @line_number: number of current operation
 * Return: void has no return value
 */
void m_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
